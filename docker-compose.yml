services:
  themist:
    build:
      context: .
    container_name: themist
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3001
    ports:
      - "127.0.0.1:3001:3001"
    volumes:
      - ./data:/app/data
